# Environment Configuration Example
# Copy this file to .env and update with your actual values

# ========================================
# Application Status
# ========================================
# Values: dev | prod
VITE_APP_STATUS=dev

# ========================================
# API Configuration
# ========================================
# Base URL for API endpoints
# Development: http://localhost:3000/api/v1/
# Production: https://api.aps.dz/api/v1/
VITE_BASE_URL=http://localhost:3000/api/v1/

# Base URL for images and media files
# Development: http://localhost:3000
# Production: https://api.aps.dz
VITE_IMAGE_URL=http://localhost:3000

# ========================================
# Application Settings
# ========================================
# API key for backend authentication
# Generate a strong, unique key for each environment
VITE_APP_ID=your-api-key-here

# Default message for empty data states
VITE_EMPTY_DATA=No data available

# ========================================
# Security Configuration
# ========================================
# Secret key for AES encryption (minimum 32 characters)
# IMPORTANT: Use a strong, random key in production
# Generate with: openssl rand -base64 32
VITE_KEY=your-aes-encryption-secret-key-min-32-chars

# Prefix for localStorage keys
# Used to avoid conflicts with other applications
VITE_PREF=_aps_

# ========================================
# Development Settings (Optional)
# ========================================
# Enable debug mode (only in development)
# VITE_DEBUG=true

# Enable verbose logging
# VITE_VERBOSE=false

# ========================================
# Production Checklist
# ========================================
# Before deploying to production, ensure:
# [ ] VITE_APP_STATUS is set to 'prod'
# [ ] VITE_BASE_URL points to production API
# [ ] VITE_IMAGE_URL points to production server
# [ ] VITE_APP_ID is a strong, unique production key
# [ ] VITE_KEY is a strong, random 32+ character secret
# [ ] All URLs use HTTPS
# [ ] This file is NOT committed to version control
